# Apple App Store Signing Configuration
# Copy this file to .env and fill in your actual values

# App Store Connect - App's numeric ID (NOT your personal Apple ID email)
APPLE_ID_NUMERIC=1234567890

# Optional: patch versions appended to VERSION.txt
IOS_PATCH_VERSION=0
MACOS_PATCH_VERSION=0

# macOS App Store Signing Configuration
# Path to macOS provisioning profile from App Store Connect
MACOS_PROFILE_PATH=/path/to/your/macOS.provisionprofile

# Certificate names (must match certificates in your keychain exactly)
# Use: security find-identity -v -p codesigning
# Team: Your Team Name (TEAM_ID)
MACOS_CERTIFICATE_NAME="Apple Distribution: Your Team Name (TEAM_ID)"
MACOS_INSTALLER_CERTIFICATE_NAME="3rd Party Mac Developer Installer: Your Team Name (TEAM_ID)"

# Optional: Override default paths
# APP_PATH=$(pwd)/output/MACOS/app/XCSoar.app
# SIGNED_PKG_PATH=$(pwd)/output/MACOS/XCSoar-signed.pkg

# iOS App Store Signing Configuration
# Path to iOS provisioning profile from App Store Connect
IOS_PROFILE_PATH=/path/to/your/iOS.mobileprovision

# iOS signing certificate name (must match certificate in your keychain exactly)
IOS_CERTIFICATE_NAME="Apple Distribution: Your Team Name (TEAM_ID)"

# Optional: Override default iOS paths
# IOS_IPA_PATH="$(pwd)/output/IOS64/xcsoar.ipa"
# IOS_SIGNED_IPA_PATH="$(pwd)/output/IOS64/xcsoar-signed.ipa"

# iOS Device Installation
# Device name for installing the app (find with: xcrun devicectl list devices)
IOS_DEVICE_NAME="Your Device Name"
