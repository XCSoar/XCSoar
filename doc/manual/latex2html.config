# latex2html preserves the TEXINPUTS environment variable, but its
# helper program texexpand doesn't use it.  This is a kludge to work
# around this limitation.

$TEXINPUTS = "doc/manual/en${envkey}doc/manual";
