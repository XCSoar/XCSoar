<?xml version="1.0" encoding="UTF-8"?>
<document>
        <metadata>
                <title>File Type - Profile</title>
        </metadata>

<section>

		<title>File Type - Profile</title>

<p>
Profile files can be used to store configuration settings used by XCSoar.
The format is a simple text file containing &lt;label&gt;=&lt;value&gt; pairs.
Certain values are text strings delimited by double quotes, for example:
</p>

<p>TODO: Proper formatting of code section!</p>

<code>
PilotName="Baron Richtoffen"
</code>

<p>
All other values are numeric, including ones that represent boolean
values (true=1, false=0), for example:
</p>

<code>
StartDistance=1000
</code>

<p>
All values that have physical dimensions are expressed in SI units
(meters, meters/second, seconds etc).
</p>

<p>
When a profile file is saved, it contains all configuration settings.
Profile files may be edited with a text editor to produce a smaller
set of configuration settings that can be given to other pilots to
load.  
</p>

<p>
When a profile file is loaded, only the settings defined in the file
overwrite the configuration settings in XCSoar; all other settings are
unaffected.
</p>


	<section>
		<title>Advanced - Programming</title>
		<p>
		</p>
	</section>
</section>
</document>

	
