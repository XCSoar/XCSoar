# frozen_string_literal: true

source "https://rubygems.org"

gem "fastlane", "~> 2.235"
# representable (fastlane → google-apis) requires multi_json at runtime; pin
# explicitly so RubyGems cannot skip it during transitive resolution.
gem "multi_json", ">= 1.14.1"
