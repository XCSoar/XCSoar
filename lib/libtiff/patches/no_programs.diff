Index: tiff-4.0.6/libtiff/Makefile.am
===================================================================
--- tiff-4.0.6.orig/libtiff/Makefile.am
+++ tiff-4.0.6/libtiff/Makefile.am
@@ -145,9 +145,9 @@ libtiffxx_la_DEPENDENCIES = libtiff.la
 # are generated by the mkg3states program.  On systems without
 # make these rules have to be manually carried out.
 #
-noinst_PROGRAMS = mkg3states
-mkg3states_SOURCES = mkg3states.c tif_fax3.h
-mkg3states_LDADD = $(LIBPORT)
+#noinst_PROGRAMS = mkg3states
+#mkg3states_SOURCES = mkg3states.c tif_fax3.h
+#mkg3states_LDADD = $(LIBPORT)
 
 faxtable: mkg3states
 	(rm -f tif_fax3sm.c && ./mkg3states -b -c const tif_fax3sm.c)
Index: tiff-4.0.6/Makefile.am
===================================================================
--- tiff-4.0.6.orig/Makefile.am
+++ tiff-4.0.6/Makefile.am
@@ -61,7 +61,7 @@ distcheck-hook:
 	rm -rf $(distdir)/_build/cmake
 	rm -rf $(distdir)/_inst/cmake
 
-SUBDIRS = port libtiff tools build contrib test man html
+SUBDIRS = port libtiff
 
 release:
 	(rm -f $(top_srcdir)/RELEASE-DATE && echo $(LIBTIFF_RELEASE_DATE) > $(top_srcdir)/RELEASE-DATE)
