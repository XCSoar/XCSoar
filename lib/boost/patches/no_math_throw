Index: boost_1_92_0/boost/math/policies/error_handling.hpp
===================================================================
--- boost_1_92_0.orig/boost/math/policies/error_handling.hpp
+++ boost_1_92_0/boost/math/policies/error_handling.hpp
@@ -27,8 +27,8 @@
 #include <cstdint>
 #include <cstring>
 #ifndef BOOST_MATH_NO_EXCEPTIONS
-#include <iomanip>
-#include <sstream>
+//#include <iomanip>
+//#include <sstream>
 #include <stdexcept>
 #endif
 #include <string>
@@ -97,6 +97,7 @@ namespace detail
 
 #ifndef BOOST_MATH_NO_EXCEPTIONS
 // Only used to build exception messages; relies on <sstream>
+#if 0
 template <class T>
 inline std::string prec_format(const T& val)
 {
@@ -115,6 +116,7 @@ inline std::string prec_format(const T&
 
    return strm.str();
 }
+#endif
 
 #ifdef BOOST_MATH_USE_CHARCONV_FOR_CONVERSION
 
@@ -190,6 +192,7 @@ void raise_error(const char* pfunction,
   BOOST_MATH_THROW_EXCEPTION(E(msg))
 }
 
+#if 0
 template <class E, class T>
 void raise_error(const char* pfunction, const char* pmessage, const T& val)
 {
@@ -236,6 +239,7 @@ BOOST_MATH_GPU_ENABLED inline T raise_do
    return boost::math::numeric_limits<T>::quiet_NaN();
 #endif
 }
+#endif
 
 template <class T>
 BOOST_MATH_GPU_ENABLED constexpr T raise_domain_error(
@@ -249,6 +253,7 @@ BOOST_MATH_GPU_ENABLED constexpr T raise
    return boost::math::numeric_limits<T>::quiet_NaN();
 }
 
+#if 0
 template <class T>
 BOOST_MATH_GPU_ENABLED inline T raise_domain_error(
            const char* ,
@@ -285,6 +290,7 @@ BOOST_MATH_GPU_ENABLED inline T raise_po
    return boost::math::policies::detail::raise_domain_error(function, message, val,  ::boost::math::policies::domain_error< ::boost::math::policies::throw_on_error>());
 #endif
 }
+#endif
 
 template <class T>
 BOOST_MATH_GPU_ENABLED constexpr T raise_pole_error(
@@ -316,6 +322,7 @@ BOOST_MATH_GPU_ENABLED inline T raise_po
    return user_pole_error(function, message, val);
 }
 
+#if 0
 template <class T>
 BOOST_MATH_GPU_ENABLED inline T raise_overflow_error(
            const char* function,
@@ -330,7 +337,9 @@ BOOST_MATH_GPU_ENABLED inline T raise_ov
    return boost::math::numeric_limits<T>::has_infinity ? boost::math::numeric_limits<T>::infinity() : boost::math::tools::max_value<T>();
 #endif
 }
+#endif
 
+#if 0
 template <class T>
 BOOST_MATH_GPU_ENABLED inline T raise_overflow_error(
            const char* function,
@@ -346,7 +355,9 @@ BOOST_MATH_GPU_ENABLED inline T raise_ov
    return boost::math::numeric_limits<T>::has_infinity ? boost::math::numeric_limits<T>::infinity() : boost::math::tools::max_value<T>();
 #endif
 }
+#endif
 
+#if 0
 template <class T>
 BOOST_MATH_GPU_ENABLED constexpr T raise_overflow_error(
            const char* ,
@@ -443,6 +454,7 @@ BOOST_MATH_GPU_ENABLED constexpr T raise
    // to be ignored so here we go anyway:
    return T(0);
 }
+#endif
 
 template <class T>
 BOOST_MATH_GPU_ENABLED inline T raise_underflow_error(
@@ -516,6 +528,7 @@ BOOST_MATH_GPU_ENABLED inline T raise_de
    return user_denorm_error(function, message, val);
 }
 
+#if 0
 template <class T>
 BOOST_MATH_GPU_ENABLED inline T raise_evaluation_error(
            const char* function,
@@ -531,6 +544,7 @@ BOOST_MATH_GPU_ENABLED inline T raise_ev
    return T(0);
 #endif
 }
+#endif
 
 template <class T>
 BOOST_MATH_GPU_ENABLED constexpr T raise_evaluation_error(
@@ -567,6 +581,7 @@ BOOST_MATH_GPU_ENABLED inline T raise_ev
    return user_evaluation_error(function, message, val);
 }
 
+#if 0
 template <class T, class TargetType>
 BOOST_MATH_GPU_ENABLED inline TargetType raise_rounding_error(
            const char* function,
@@ -583,6 +598,7 @@ BOOST_MATH_GPU_ENABLED inline TargetType
    return TargetType(0);
 #endif
 }
+#endif
 
 template <class T, class TargetType>
 BOOST_MATH_GPU_ENABLED constexpr TargetType raise_rounding_error(
@@ -623,6 +639,7 @@ BOOST_MATH_GPU_ENABLED inline TargetType
    return user_rounding_error(function, message, val, t);
 }
 
+#if 0
 template <class T, class R>
 BOOST_MATH_GPU_ENABLED inline T raise_indeterminate_result_error(
            const char* function,
@@ -639,6 +656,7 @@ BOOST_MATH_GPU_ENABLED inline T raise_in
    return boost::math::numeric_limits<T>::quiet_NaN();
 #endif
 }
+#endif
 
 template <class T, class R>
 BOOST_MATH_GPU_ENABLED inline constexpr T raise_indeterminate_result_error(
