---
title: Task manager
description: The task manager with its task calculator, turn points, manage and rules pages.
---

::gesture-note{id="RD"}
The task manager is used to edit, view, load, save to file, and declare cross
country tasks.
::

`Nav 1/2` → `Task`

The primary page is a calculator. It shows various calculations
related to the active task, as described in detail below.  In addition, there
are buttons for invoking dialogues `Turn Points`, `Manage`,
and `Rules`, as well as a button to `Close` the task manager.

## Task calculator dialog

The task calculator dialogue allows the pilot to see the effect of
various changes to the task on final performance.

`Calculator`

In flight the Task Calculator might also be accessed from the Analysis pages.
With pages Task, Climb, and Task Speed a button `Task Calc` comes up,
that directly changes to the Task Calculator screen.

![Task calculator](/img/figures/dialog-taskcalculator.png)

::field-group
  :::field{name="Estimated task time"}
  This field displays the estimated total time
  on task to complete the task at the provided MacCready setting.
  :::

  :::field{name="Remaining task time"}
  This field displays the time remaining to complete the task.
  :::

  :::field{name="Task distance"}
  This field displays the total task distance.
  :::

  :::field{name="Remaining distance"}
  This field displays the remaining distance to complete the task.
  :::

  :::field{name="Speed estimated"}
  This field displays the estimated average cross-country
  speed for the task as of task completion, assuming flying the rest of the task
  at the provided MacCready setting.
  :::

  :::field{name="Speed average"}
  <!-- TODO: To define -->
  :::

  :::field{name="Set MacCready"}
  Allows the user to adjust the MacCready value and
  see the effect it has on the estimated task time.
  :::

  :::field{name="AAT range"}
  Allows the user to adjust the targets within the remaining
  AAT areas, to see the effect it has on estimated task time and task distance.
  :::

  :::field{name="Speed remaining"}
  This field displays the estimated speed for the
  remainder of the task at the provided MacCready setting.
  :::

  :::field{name="Achieved MacCready"}
  This field displays the achieved MacCready value.
  :::

  :::field{name="Achieved speed"}
  This field displays the achieved speed
  <!-- TODO at the achieved MacCready setting??? -->
  :::

  :::field{name="Cruise efficiency"}
  100 % indicates perfect MacCready performance, greater
  than 100 % indicates better than MacCready performance is achieved through
  flying in streets. Less than 100 % is appropriate if you fly considerably
  off-track. This value estimates your cruise efficiency according to the
  current flight history with the set MC value. Calculation begins after task
  is started.
  :::
::

See Section
[Task speed estimation](/manual/glide-computer/task-speed-estimation) for
more details on task speed and achieved MacCready calculations.

On closing the dialogue the entered MacCready value is used as the MacCready
setting. If the `Cancel` button is pushed, the MacCready setting is
unaffected.

The `Target` button, for AAT tasks, adjusts the range
(increases or decreases) so that the estimated task time exceeds the
assigned task time by less than five minutes.  The range is adjusted
target-wise. In typical use, all targets are set to “auto” that means the pilot
does not have to manually adjust the range to find the course for arrival at
the assigned task time, thereby reducing pilot workload.

## Turn Points

This page displays an ordered list of the points in the current active task.

`Turn Points`

If there are no waypoints in the active task, there
will be only an option to “Add Turnpoint.”  By highlighting (tapping) the
“Add Turnpoint” function, then tapping in the highlighted region, the waypoint
selector is displayed, as described above.  Highlighting a waypoint from the
list and then tapping on the highlighted region adds the waypoint to the task.

## Manage

This page allows for invoking all operations needed to create new or manage
already defined tasks.

`Manage`

::field-group
  :::field{name="New Task"}
  Clears the current task and resets the task rules to
  the default values.
  :::

  :::field{name="Declare"}
  If an external logger is connected, this will allow
  uploading the  active task to the logger and declaring it.
  :::

  :::field{name="Browse"}
  Displays a list of all the saved tasks, allowing the
  pilot to load a previously saved task.  Note that this option will overwrite
  the current active task.
  :::

  :::field{name="Save"}
  Saves the current active task.  Upon tapping the
  `Save` button the pilot will be prompted to enter a file name for the
  task to be saved.
  :::
::

## Rules

The values in the menu depend on the task type selected. Clicking any existing
value will bring up another menu allowing the pilot to select a
different value for this rule.  Task types are discussed in more detail below.

`Rules`

Also, tapping on the `Rules` button again after it is highlighted allows
toggling between a “thumbnail” view of the task map and a larger view of the
task.
