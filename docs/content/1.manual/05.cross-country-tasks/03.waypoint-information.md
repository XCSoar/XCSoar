---
title: Waypoint Information
description: The waypoint info dialogue with details, altitude differences, task actions, airfield information and images.
---

The waypoint info dialogue describes a waypoint in detail and has
navigation functions such as GoTo, insert, append to the task, or set the
waypoint as the new home.
This may be accessed in several ways:

::gesture-note{id="RD"}
or menu `Nav 1/2` → `Task`,
<!-- TODO descr. tabular “Turn Points” -->
highlight a waypoint, then tap the highlighted waypoint again to display the
task point dialogue, then push `Details`.
::

::gesture-note{id="DL"}
or menu `Nav 1/2` → `Alternates`
to show the
details of the landables nearest to the aircraft.
::

::gesture-note{id="DR"}
or menu `Nav 1/2` → `Waypoint List`
, highlight and `select` a waypoint to show its details.
::

::gesture-note{id="URDL"}
or menu `Display 1/2` → `Pan On` to put the
map into pan mode, pan to the desired waypoint, touch its name or the waypoint
symbol.
::

`Info 1/3` → `What's here?`
to show the
item list under the cross-hair, or your index finger on the map.

The airfields or waypoint details dialogue contains two major pages (accessed
via the `>` and `<` buttons). Depending on the availability of further
details of the waypoint they will by shown on extra pages.

## Waypoint details

This page contains text giving the name of the waypoint,
the waypoint’s GPS coordinates, elevation, radio frequency and
runway information (if this information is in the waypoint file),
local daylight time, bearing and distance to the waypoint, and the altitude
differences required
to reach the waypoint as described below. In addition, there is a button
`GoTo` to directly initiate
navigating to this waypoint. The button cancels the current task.

![Waypoint details](/img/figures/dialog-waypointdetails0.png)

As mentioned above, the Waypoint info dialogue also shows three forms of
altitude difference (additional
altitude required to reach the waypoint at the safety height) for
the corresponding waypoint:

::field-group
  :::field{name="Alt diff MC 0"}
  Altitude difference at MC setting of 0
  :::

  :::field{name="Alt diff MC safety"}
  Altitude difference at the abort/safety MacCready
  setting (see
  [Speed to fly with risk](/manual/glide-computer/speed-to-fly-with-risk))
  :::

  :::field{name="Alt diff MC current"}
  Altitude difference at the current MacCready setting
  :::
::

From the main Waypoint Info screen, you can access the second page by using the
`>` and `<` buttons in the bottom left corner of the page.
Depending of the type and number of details available for a waypoint,
other pages could be available.

## Task actions menu

This page contains a column of buttons allowing various actions to be performed:

::field-group
  :::field{name="Replace in task"}
  replaces the active waypoint in the task with
  the selected waypoint.
  :::

  :::field{name="Insert in task"}
  inserts the selected waypoint before the active
  waypoint in the task.
  :::

  :::field{name="Append to task"}
  adds the selected waypoint to the end of the task.
  :::

  :::field{name="Remove from task"}
  removes the selected waypoint from the task.
  Note that this option is only visible if the selected waypoint is included in
  the active task.
  :::

  :::field{name="Set as new home"}
  sets the waypoint as the home airfield.
  :::

  :::field{name="Pan to waypoint"}
  switches to pan mode and pans to that waypoint.
  :::

  :::field{name="Set Active Frequency"}
  <!-- TODO To define -->
  :::

  :::field{name="Set Standby Frequency"}
  <!-- TODO To define -->
  :::

  :::field{name="Edit"}
  allows to change the various attributes of the selected waypoint
  :::
::

It is a good idea to set your home waypoint from the airfields or waypoint
details dialogue. This causes XCSoar to start up at the home location
regardless of whether a GPS fix is received.  If no home is set, then XCSoar
starts in the centre of the terrain map.

## Airfield information

This page may contain relevant text from the en-route supplement about
the airfield, including runways, radio frequencies, traffic patterns,
contacts.
See Section
[Airfields or Waypoint details](/manual/data-files/airfields-or-waypoint-details).

![Airfield information](/img/figures/dialog-waypointdetails1.png)

## Waypoint details image

This page shows a satellite image of the waypoint.

![Waypoint details image](/img/figures/dialog-waypointdetails2.png)

How to set up the detailed waypoint information read Section
[Airfields or Waypoint details](/manual/data-files/airfields-or-waypoint-details).
