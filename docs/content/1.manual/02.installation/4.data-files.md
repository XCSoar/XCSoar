---
title: Data files
description: Where the XCSoarData directory lives on each platform and which files XCSoar creates there at run time.
---

To be able to use XCSoar’s advanced features, additional data files, such as
terrain, topography, special use airspace, waypoints etc. are needed. The files
that can be used with XCSoar are described in Chapter
[Data Files](/manual/data-files/file-management).

All data files should be copied into the directory `XCSoarData`.  This directory
must be in a specific place so that XCSoar knows where to look for data files:

::field-group
  :::field{name="Windows PC"}
  `XCSoarData` is in your personal folder (“`My Documents`”)
  :::

  :::field{name="Windows Mobile PDA/PNA"}
  If there is a directory named `XCSoarData` in the same directory as
  `XCSoar.exe`, then this one will be used. `XCSoarData` is on the SD card.  If
  there is no SD card, then XCSoar looks for it in `My Documents`.
  :::

  :::field{name="Unix/Linux"}
  The directory is called `.xcsoar` in the user’s home directory.
  :::

  :::field{name="Android Devices"}
  `XCSoarData` is on the SD card.
  :::
::

XCSoar will generate a number of additional files at run time.  These will be
placed in the `XCSoarData` directory (Windows PC, Windows and Android mobile
devices), or the `.xcsoar` directory (Unix/Linux PC).  From the first run,
XCSoar will create and maintain the files `user.cup` (user-edited waypoints),
`Default.tsk` (Default Task), `default.prf` (configuration settings),
`xcsoar.log`, plus three directories: `cache`, `config` and `logs`.  Additional
files may be created/modified while XCSoar is running, such as task files
(`*.tsk`) and flight logs.
