{
  "name": "xcsoar-docs",
  "scripts": {
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "build": "nuxt build",
    "pdf": "node scripts/build-pdf.mjs",
    "build:docs": "npm run generate && npm run pdf -- manual && npm run pdf -- quick-guide"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "docus": "latest",
    "nuxt": "^4.5.2"
  },
  "devDependencies": {
    "pagedjs": "^0.4.3",
    "puppeteer": "^25.11.0"
  }
}
